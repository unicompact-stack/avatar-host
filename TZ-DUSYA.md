# Разбор ТЗ «Дуся / Jarvis PWA»

ТЗ **в целом верное по цели**, но **нельзя выполнять буквально** как клон `Priler/jarvis` + Tauri + Vosk + Rustpotter.

## Что не так

1. **Конфликт цели и стека.** Цель — «открыть в браузере / PWA на телефоне». Tauri — это десктоп/обёртка, не вкладка Chrome. На телефоне Tauri = отдельная история (не PWA).
2. **Шаг 0 (`cargo tauri build`) не про PWA.** Сборка нативного бинарника не даёт адаптивный сайт.
3. **Vosk в Rust на телефоне в браузере не работает.** Браузер не отдаёт сырой микрофон в ваш Rust-бэкенд, пока нет нативного приложения. Для PWA STT — Web Speech API или WASM-модель.
4. **Porcupine с API-ключом** сам ТЗ отвергает — ок, не берём.
5. **Rustpotter** нуждается в обученном wake-word; для MVP достаточно распознать «Дуся» в потоке STT.
6. **Web Speech TTS** для MVP ок, но Chrome STT часто ходит в облако Google — это **не 100% офлайн**, вопреки формулировке ТЗ.
7. **Клонировать Jarvis в этот репозиторий** нельзя: здесь уже другой проект (аватар-хост), сессия на фиксированной ветке.

## Что сделано вместо этого

Рабочий **браузерный PWA-прототип** `dusya/`:

| Пункт чек-листа | Как |
|---|---|
| Адаптивный сайт / PWA | Vite + Svelte, manifest, service worker |
| STT кнопкой «Старт» | Web Speech API (`webkitSpeechRecognition`, ru-RU) |
| TTS «Сказать» | `speechSynthesis` |
| Wake word | Фраза «Дуся» / «окей Дуся» в распознанном тексте |
| Плагины | JSON + rule matcher; папка `plugins/` |
| Каталог / мои скрипты | вкладки, localStorage |
| Команды | время, дата, погода (эмуляция), HTTP-пинг, открыть карты |

Запуск: `cd dusya && npm i && npm run dev` → порт 5173.
